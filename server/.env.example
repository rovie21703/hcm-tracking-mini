# Server environment variables (copy to server/.env or export in your shell)

# Port for the Express API (default: 4000)
# PORT=4000

# Path to Firebase service account JSON (alternative to server/serviceAccountKey.json)
# GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/serviceAccountKey.json
